import { LangTranslatePipe } from './lang-translate.pipe';

describe('LangTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new LangTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
